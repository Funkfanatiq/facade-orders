# 🚀 Инструкция по деплою на Render.com

## ✅ Проект готов к деплою!

### 📋 Основные файлы:
- `app.py` - основное приложение Flask
- `models.py` - модели базы данных
- `config.py` - конфигурация приложения
- `requirements.txt` - зависимости Python
- `runtime.txt` - версия Python (3.11.9)
- `Procfile` - команда запуска для Render
- `render_init_db.py` - скрипт инициализации БД (опционально)

### 🔧 Настройки Render.com:

**Вариант A — Docker (рекомендуется, для ТОРГ-12 Excel→PDF):**
1. **Environment**: **Docker**
2. Build и Start — автоматически из Dockerfile (LibreOffice установлен для конвертации xlsx→pdf)

**Вариант B — Python (без ТОРГ-12 из шаблона):**
1. **Build Command**: `pip install -r requirements.txt`
2. **Start Command**: оставьте пустым (Render использует Procfile)
3. **Python Version**: 3.11.9 (указывается в runtime.txt)
4. **Environment**: Python 3

### 📊 Переменные окружения (Environment Variables):

Обязательно добавьте в настройках Render:
- `SECRET_KEY` - секретный ключ для Flask (любая случайная строка)
- `DATABASE_URL` - URL PostgreSQL базы данных от Render

**Как получить DATABASE_URL:**
1. Создайте PostgreSQL базу данных в Render Dashboard
2. **Рекомендуется:** используйте **Internal Database URL** (веб-сервис и БД в одном регионе Render — меньше проблем с SSL)
3. Либо скопируйте "External Database URL"
4. Убедитесь, что URL начинается с `postgresql://` (не `postgres://`). В `config.py` автоматически добавляется `sslmode=require` и настройки пула для Render.

### 🗄️ Инициализация базы данных:

База данных автоматически инициализируется при первом запуске приложения (функция `init_database()` в `app.py`).

Если нужно принудительно инициализировать БД, добавьте в Build Command:
```
pip install -r requirements.txt && python render_init_db.py
```

### 👤 Учетные записи по умолчанию:

После инициализации БД создаются пользователи:
- **Админ**: `admin` / `admin123`
- **Менеджер**: `manager` / `5678`
- **Производство**: `worker` / `0000`
- **Фрезеровка**: `cutter` / `7777`
- **Шлифовка**: `polisher` / `8888`
- **Монитор**: `monitor` / `9999`

### 🎯 Готово к деплою!

1. Закоммитьте изменения: `git add . && git commit -m "Prepare for Render deployment"`
2. Отправьте на GitHub: `git push origin master`
3. На Render нажмите "Manual Deploy" → "Deploy latest commit"
4. Дождитесь завершения сборки и проверьте логи

### ⚠️ Важные замечания:

- Убедитесь, что база данных PostgreSQL создана **до** деплоя веб-сервиса
- Проверьте, что DATABASE_URL правильно настроен
- При ошибке **"SSL connection has been closed unexpectedly"**: используйте **Internal Database URL** в DATABASE_URL; в коде уже добавлены `sslmode=require`, `pool_pre_ping`, `pool_recycle` и retry при старте
- После первого деплоя проверьте логи на наличие ошибок
- Файлы загружаются в папку `uploads/` (убедитесь, что она существует)
